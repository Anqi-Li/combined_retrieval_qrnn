[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "combined_retrieval_qrnn"
version = "0.1.0"
description = "Combined retrieval QRNN"
readme = "README.md"
requires-python = ">=3.8"
